@model List<Juice>
@{
    ViewData["Title"] = "Home Page";
}

<table class="table">
    <thead>
        <tr>
            <th scope="col">STT</th>
            <th scope="col">Image</th>
            <th scope="col">DrinkName</th>
            <th scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Juice juice in @Model)
        {
        <tr>
            <th scope="row">@juice.ID</th>
            <td><img style="height:80px;width:40%" asp-append-version="true" src=@juice.Image /></td>
            <td>@juice.name</td>
            <td>@juice.Price</td>
        </tr>
        }
    </tbody>
</table>
@model List<Coffee>
@{
    ViewData["Title"] = "Home Page";
}

<table class="table">
    <thead>
        <tr>
            <th scope="col">STT</th>
            <th scope="col">Image</th>
            <th scope="col">DrinkName</th>
            <th scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Coffee coffee in @Model)
        {
        <tr>
            <th scope="row">@coffee.ID</th>
            <td><img style="height:80px;width:40%" asp-append-version="true" src=@coffee.Image /></td>
            <td>@coffee.name</td>
            <td>@coffee.Price</td>
        </tr>
        }
    </tbody>
</table>